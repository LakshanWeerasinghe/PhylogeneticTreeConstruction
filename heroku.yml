build:
  docker:
    web: Dockerfile.prod
run:
  web: gunicorn app.wsgi:application --bind 0.0.0.0:8000  
    worker: 
      command:
        - celery -A app --concurrency=1 worker -l info
      image: worker