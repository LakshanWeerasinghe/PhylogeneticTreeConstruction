build:
  docker:
    web: Dockerfile.prod
run:
  web: 
    command:
      - gunicorn app.wsgi:application --bind 0.0.0.0:$PORT  
    image: web
  worker: 
    command:
      - celery -A app --concurrency=1 worker -l info
    image: web